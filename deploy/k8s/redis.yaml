apiVersion: apps/v1
kind: Deployment
metadata:
  name: driftguard-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: driftguard-redis
  template:
    metadata:
      labels:
        app: driftguard-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: driftguard-redis
spec:
  selector:
    app: driftguard-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
